// Change this single line to replace the launch animation in the future.
export const LAUNCH_VIDEO_SRC = '/thien-launch.mp4'

// Fade-out duration (ms) from the animation to the main interface.
export const LAUNCH_FADE_MS = 500

// Storage key that ensures the animation plays only once per application launch
// (per browser session/tab). Clear it in DevTools > Application to see it again.
export const LAUNCH_SESSION_KEY = 'thien:launch-played'
